# 🦞 Clawdbot 配置总结与启动指南

## 📋 已完成的配置任务

### ✅ 环境准备
- **Node.js 升级**：从 v20.20.0 升级到 v22.22.0
  - 安装位置：`/opt/homebrew/opt/node@22/bin`
  - 已添加到 PATH：`export PATH="/opt/homebrew/opt/node@22/bin:$PATH"`
  - 配置文件：`~/.zshrc`

### ✅ 依赖管理
- **修复工作区依赖**：将所有扩展包中的 `clawdbot` 引用修改为 `moltbot`
  - 修复的文件：
    - `extensions/googlechat/package.json`
    - `extensions/line/package.json`
    - `extensions/matrix/package.json`
    - `extensions/memory-core/package.json`
    - `extensions/msteams/package.json`
    - `extensions/nostr/package.json`
    - `extensions/twitch/package.json`
    - `extensions/zalo/package.json`
    - `extensions/zalouser/package.json`
- **依赖安装**：成功安装所有项目依赖（`pnpm install`）

### ✅ 项目构建
- **UI 构建**：成功构建用户界面（`pnpm ui:build`）
- **项目编译**：成功编译 TypeScript 代码（`pnpm build`）
- **生成目录**：创建了 `dist/` 目录

### ✅ 基础配置
- **配置向导**：运行了 `clawdbot onboard` 初始化配置
- **网关服务**：安装并启动了网关守护进程
- **配置文件**：创建了 `~/.clawdbot/clawdbot.json`
- **工作区**：设置了默认工作区 `~/clawd`

### ✅ 网关设置
- **端口配置**：网关运行在端口 18789
- **绑定模式**：配置为局域网访问（`bind: "lan"`）
- **认证模式**：设置为无认证（`auth.mode: "none"`）
- **Web 控制台**：可通过 http://127.0.0.1:18789/ 访问

## 🔧 需要你完成的配置

### 1. AI 模型 API 密钥配置（必需）

**选择一个 AI 提供商并配置 API 密钥：**

#### 方式 1：通过 Web 控制台（推荐）
1. 访问：http://127.0.0.1:18789/
2. 在设置中添加你的 API 密钥
3. 支持的提供商：
   - **Anthropic**（推荐）：Claude Pro/Max + Opus 4.5
   - **OpenAI**：ChatGPT/GPT-4
   - 其他兼容的提供商

#### 方式 2：通过命令行
```bash
# 进入项目目录
cd /Users/genglin/Documents/Hermeson/clawdbot
export PATH="/opt/homebrew/opt/node@22/bin:$PATH"

# 配置 Anthropic（推荐）
pnpm clawdbot configure --section model

# 或者直接设置（替换为你的实际密钥）
pnpm clawdbot config set agents.defaults.model.anthropic.apiKey "your-anthropic-api-key"
pnpm clawdbot config set agents.defaults.model.id "anthropic/claude-opus-4-5"
```

### 2. 消息通道配置（可选）

**如果你想通过消息应用与 AI 对话：**

#### Telegram Bot（最简单）
1. 在 Telegram 中找到 @BotFather
2. 发送 `/newbot` 创建新 bot
3. 获得 Bot Token
4. 配置：
```bash
pnpm clawdbot config set channels.telegram.botToken "YOUR_BOT_TOKEN"
```

#### WhatsApp（需要扫码）
```bash
pnpm clawdbot channels login --channel whatsapp
```

#### Discord Bot
```bash
pnpm clawdbot config set channels.discord.token "YOUR_DISCORD_BOT_TOKEN"
```

### 3. 安卓手机通信配置（可选）

**如果你想在安卓手机上使用：**

#### 方式 1：局域网 Web 访问
- 确保手机和电脑在同一 WiFi
- 在手机浏览器访问：`http://10.26.132.80:18789/`

#### 方式 2：构建 Android 应用
```bash
pnpm android:assemble
pnpm android:install  # 需要连接安卓设备
```

## 🚀 项目启动命令

### 日常启动流程

#### 1. 启动网关服务
```bash
# 进入项目目录
cd /Users/genglin/Documents/Hermeson/clawdbot

# 设置 Node.js 路径
export PATH="/opt/homebrew/opt/node@22/bin:$PATH"

# 启动网关（后台运行）
pnpm clawdbot gateway start

# 或者前台运行（用于调试）
pnpm clawdbot gateway run --bind lan --port 18789
```

#### 2. 检查服务状态
```bash
# 检查网关状态
pnpm clawdbot gateway status

# 检查整体状态
pnpm clawdbot status

# 检查通道状态
pnpm clawdbot channels status
```

#### 3. 访问 Web 控制台
- 打开浏览器访问：http://127.0.0.1:18789/
- 或局域网访问：http://10.26.132.80:18789/

### 开发模式启动

#### 1. 开发环境启动（自动重载）
```bash
cd /Users/genglin/Documents/Hermeson/clawdbot
export PATH="/opt/homebrew/opt/node@22/bin:$PATH"

# 启动开发模式（TS 文件变化时自动重载）
pnpm gateway:watch
```

#### 2. 重新构建项目
```bash
# 重新构建 UI
pnpm ui:build

# 重新构建项目
pnpm build
```

### 测试和调试

#### 1. 测试 AI 对话
```bash
# 测试基本对话（需要先配置 API 密钥）
pnpm clawdbot agent --session-id test --message "你好，测试消息" --thinking low
```

#### 2. 发送消息到通道
```bash
# 发送到 Telegram（需要先配置 bot token）
pnpm clawdbot message send --to telegram --message "Hello from Clawdbot"
```

#### 3. 诊断和修复
```bash
# 运行诊断工具
pnpm clawdbot doctor

# 修复配置问题
pnpm clawdbot doctor --fix
```

## 📁 重要文件和目录

### 配置文件
- **主配置**：`~/.clawdbot/clawdbot.json`
- **凭据存储**：`~/.clawdbot/credentials/`
- **会话数据**：`~/.clawdbot/agents/main/sessions/`

### 项目目录
- **源代码**：`src/`
- **构建输出**：`dist/`
- **UI 代码**：`ui/`
- **扩展包**：`extensions/`
- **文档**：`docs/`

### 工作区
- **默认工作区**：`~/clawd`
- **技能目录**：`~/clawd/skills/`
- **提示文件**：`~/clawd/AGENTS.md`, `~/clawd/SOUL.md`, `~/clawd/TOOLS.md`

## 🔧 常用维护命令

### 服务管理
```bash
# 启动服务
pnpm clawdbot gateway start

# 停止服务
pnpm clawdbot gateway stop

# 重启服务
pnpm clawdbot gateway restart

# 重新安装服务
pnpm clawdbot gateway install
```

### 配置管理
```bash
# 查看配置
pnpm clawdbot config get

# 设置配置
pnpm clawdbot config set key value

# 重新运行配置向导
pnpm clawdbot onboard
```

### 更新和维护
```bash
# 更新到最新版本
pnpm clawdbot update

# 检查健康状态
pnpm clawdbot doctor

# 查看日志
tail -f /tmp/clawdbot/clawdbot-$(date +%Y-%m-%d).log
```

## 🆘 故障排除

### 常见问题

#### 1. 网关无法启动
```bash
# 检查端口占用
lsof -i :18789

# 强制停止旧进程
pkill -f clawdbot-gateway

# 重新启动
pnpm clawdbot gateway start
```

#### 2. API 密钥问题
```bash
# 检查配置
pnpm clawdbot config get agents.defaults.model

# 重新配置
pnpm clawdbot configure --section model
```

#### 3. 权限问题
```bash
# 检查 Node.js 版本
node --version

# 重新设置 PATH
export PATH="/opt/homebrew/opt/node@22/bin:$PATH"
```

### 获取帮助
- **文档**：https://docs.clawd.bot
- **GitHub**：https://github.com/clawdbot/clawdbot
- **Discord**：https://discord.gg/clawd

## 📝 下次启动检查清单

1. ✅ 确认 Node.js 版本（≥22）
2. ✅ 设置正确的 PATH
3. ✅ 进入项目目录
4. ✅ 启动网关服务
5. ✅ 检查服务状态
6. ✅ 访问 Web 控制台
7. ✅ 确认 API 密钥配置
8. ✅ 测试基本功能

---

**注意**：这个配置总结基于当前的项目状态。如果你修改了配置或遇到问题，请参考相应的故障排除部分或查阅官方文档。